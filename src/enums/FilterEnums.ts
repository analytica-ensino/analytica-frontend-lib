/**
 * Filter category keys enum
 */
export enum FILTER_CATEGORY {
  SCHOOL = 'school',
  SCHOOL_YEAR = 'schoolYear',
  CLASS = 'class',
  SUBJECT = 'subject',
  STATUS = 'status',
}

/**
 * Filter group keys enum
 */
export enum FILTER_GROUP {
  ACADEMIC = 'academic',
  CONTENT = 'content',
  STATUS = 'status',
}
